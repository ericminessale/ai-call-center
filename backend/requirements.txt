# Core Dependencies 
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
Flask-CORS==6.0.0
# 5.6.1, NOT 5.5.1: 5.5.1 assigns `ctx.session`, which Flask 3.1 made a
# read-only property. Every Socket.IO CONNECT handler raised
# "AttributeError: property 'session' of 'RequestContext' object has no setter",
# so no client joined any room and ALL realtime was silently dead. 5.6.1 assigns
# `ctx._session` when present (upstream comment: "for Flask >= 3.1.3"). If Flask
# is ever pinned back below 3.1, this may move too — they go together.
Flask-SocketIO==5.6.1
python-socketio[client]==5.16.2
Flask-Bcrypt==1.0.1 
 
# Database 
psycopg2-binary==2.9.9 
SQLAlchemy==2.0.23 
alembic==1.13.1 
 
# Authentication
PyJWT==2.13.0
Flask-JWT-Extended==4.7.1
cryptography==48.0.1
 
# Redis 
redis==5.0.1 
hiredis==2.2.3 
 
# SignalWire SDK — canonical REST client for Fabric, phone numbers, DataSphere
signalwire-sdk>=3.0.0,<4.0.0

# Twilio
twilio==8.10.3
 
# Server 
gunicorn==23.0.0
# Only used by the Dockerfile's `--worker-class eventlet` fallback CMD; nothing
# in app/ imports it and docker-compose runs the threaded sync worker instead
# (matching socketio async_mode='threading').
eventlet==0.40.3

# Utilities
python-dotenv==1.2.2
Faker==20.1.0

# WebSocket (for SignalWire tap audio relay)
flask-sock==0.7.0
simple-websocket==1.0.0

# HTTP Requests (bumped to satisfy signalwire-sdk >= 2.32.3 floor)
requests>=2.32.3,<3.0.0
marshmallow==3.26.2
python-dateutil==2.8.2

# Testing and Linting
pytest==9.0.3
pytest-flask==1.3.0
black==26.3.1
flake8==7.0.0
